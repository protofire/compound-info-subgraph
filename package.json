{
    "name": "compound-info",
    "license": "UNLICENSED",
    "scripts": {
        "codegen": "graph codegen",
        "build": "graph build",
        "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ compound-info",
        "create-local": "graph create --node http://localhost:8020/ compound-info",
        "remove-local": "graph remove --node http://localhost:8020/ compound-info",
        "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 compound-info"
    },
    "dependencies": {
        "@graphprotocol/graph-cli": "^0.29.0",
        "@graphprotocol/graph-ts": "^0.26.0"
    }
}
